import React from 'react'
import './afterScale.css'

function AfterScaleCart({title, desc}) {
  return (
    <div className="afterScaleCart">
      <h1>{title}</h1>
      <p>{desc}</p>
    </div>
  )
}

export default AfterScaleCart